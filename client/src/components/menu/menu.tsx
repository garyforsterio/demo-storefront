import * as React from 'react';

import { MenuItem } from '/index.d';

type MenuProps = {
  activePath?: string;
  items: MenuItem[];
};

const Menu: React.FunctionComponent<MenuProps> = () => {
  return null;
};

export default Menu;
